# Dx12Engine 개발 로드맵

DirectX 12 + C++ 기반 게임 엔진의 개발 순서 문서.

`v1.3`까지로 엔진의 기반 — ECS 런타임, 렌더러, 에디터, 독립 실행 Player,
패키징 — 은 서 있다. 여기서부터는 **만들 게임 하나를 정해 놓고, 그 게임이
요구하는 것만 만든다.** 기능 목록을 위에서 아래로 훑는 방식이 아니다.

## 진행 원칙

1. **매 마일스톤은 플레이 가능한 결과물로 끝난다** — 코드만 쌓지 않고, 마일스톤
   마다 실제로 해 본다. 게임이 forcing function이라는 것은 "언젠가 게임을 만든다"는
   뜻이 아니라 **매 단계가 게임의 한 상태**라는 뜻이다.

2. **트리거 대기 — 미리 만들지 않고, 조건이 실제로 발생할 때 착수한다.** 이 원칙은
   지금까지 두 번 정확히 맞았다(멀티 머티리얼 Submesh, 메시 AABB). 예고한 조건이
   예고한 시점에 일어났고, 미리 만들었다면 틀린 모양으로 만들었을 것이다.

   **예외는 하나** — 조건이 발생하는 순간 예외를 던지며 **죽는** 상한은 죽기 전에
   정리한다. 성능이 나빠지는 것과 실행이 중단되는 것은 다른 문제다.

3. **외부 의존성은 도입하되, 빌드 체계는 지킨다.** 포맷 파서·툴체인·검증된 런타임은
   가져온다. 조건은 `ThirdParty/`에 버전을 고정해 벤더링하고, 수제 `.vcxproj`로
   기존 `Engine/BuildSettings.props` 체계에 편입하는 것 — **CMake도 vcpkg도 들이지
   않는다.** ImGui가 이미 그 선례다.

   판단 기준은 "직접 짜는 것이 훌륭한가"가 아니라 **그 라이브러리가 자기 아키텍처를
   들고 오는가**다. glTF/FBX 파서와 셰이더 컴파일러는 가치가 전부 "스펙을 정확히
   따르는 것"에 있어 설계 여지가 없다. 물리처럼 자기 모델을 들고 오는 것은 그
   모델을 받아들일지를 따로 판단한다.

4. **에셋 라이선스 경계** — 저장소에 커밋할 수 없는 에셋은 로컬 전용으로 두고
   `.gitignore`와 `Engine/Tests/PlayerPackage.contents` 화이트리스트 양쪽에서
   제외한다. `laevat`이 이미 그 패턴이다. 여기서 따라오는 요구가 하나 있다 —
   **배포 검증은 그 에셋 없이도 통과해야 한다.** 그렇지 않으면
   `VerifyPlayerPackage.ps1`이 개발 머신에서만 도는 테스트가 된다.

5. **커밋은 작게, 마일스톤 완료 시 태그.** 회귀 푸터(`회귀: 테스트 N/N, ...`)를
   유지한다. 분석적 예측과 실측을 대조하는 습관이 실제 결함을 반복해서 잡아냈다.

---

## 지금까지 — v1.0 ~ v1.3

Phase 0~12로 아래를 완료했다. 각 단계의 세부 내용은 커밋 히스토리와 태그에 있다.

| Phase | 주제 | 태그 |
|------|------|------|
| 0~3 | 프로젝트 셋업, Win32 창, DX12 초기화, 첫 삼각형 | — |
| 4~7 | 버퍼·3D 변환, 텍스처, 카메라·입력·시간, Blinn-Phong 조명 | — |
| 8 | OBJ 로더 (멀티 머티리얼 submesh 포함) | — |
| 9 | 엔진 구조화 — Frame Resource, Descriptor 할당자, ResourceManager, ECS, ImGui | `v1.0` |
| 10 | 맵 에디터 — 오프스크린 RT, Asset Browser, Inspector, 씬 저장/불러오기 | `v1.1` |
| 11 | 중급 렌더링 — 스카이박스, 노멀 매핑, 그림자 매핑 + PCF, 알파 블렌딩, MSAA | `v1.2` |
| 12 | 플레이 모드 & 게임 런타임 — Editor/Player 분리, 패키징, 독립 배포 검증 | `v1.3` |

### 살아있는 계약 — 산출물 경계

Phase 12가 확정한 이 표는 기록이 아니라 **앞으로의 모든 마일스톤이 지켜야 하는
제약**이다. 새 코드를 어느 프로젝트에 넣을지는 매번 이 표로 판단한다.

| 산출물 | 책임 | 의존성 규칙 |
|--------|------|-------------|
| `Engine.lib` | Win32 루프, ECS 기반, 렌더러, 리소스·저수준 런타임 | Game·Editor·ImGui를 모름 |
| `Game.lib` | 공유 Component, 씬 포맷, Always/Play-only 시스템, 게임 규칙 | Engine에만 의존 |
| `Editor.exe` | Editor-only 시스템, 편집 카메라, 선택·Inspector·Asset Browser | Engine + Game + ImGui |
| `Player.exe` | 시작 Scene 로드, 게임 입력·시스템, swap-chain presentation | Engine + Game, ImGui 링크 금지 |

이 경계는 규칙이 아니라 **빌드 실패로 강제된다** — `VerifyPhase12ProjectBoundary.ps1`이
프로젝트 파일에서 유도한 화이트리스트로 검사하므로, 새 파일을 잘못된 프로젝트에
넣으면 빌드가 멈춘다. 세부 계약은 [Engine/Docs/](Engine/Docs/)에 있다.

---

## 목표 — 만들 게임

**탑다운 3D 뱀파이어 서바이벌.** 자동 공격, 시간에 따라 거세지는 적 웨이브,
XP·레벨업 선택, 생존 타이머. 아레나는 절차적으로 구성하고 프랩을 배치한다.

에셋은 이미 있는 캐릭터 FBX + 애니메이션 클립(걷기·뛰기·점프·공격)을 쓴다.
무기 모델과 VFX 소스는 없다.

### 왜 이 게임인가

장르가 재미있어서가 아니라, **이 엔진의 알려진 약점을 전부 게임 요구사항으로
바꾸기 때문이다.** 수백~수천 엔티티가 장르의 전제라, 아래 벽들이 투기적으로가
아니라 **게임이 요구해서** 터진다. 트리거 원칙과 충돌하지 않고 기반이 강화된다.

| 엔진의 벽 | 무엇이 발화시키는가 |
|---|---|
| `kMaxObjects = 256` (`Graphics/FrameResource.h`) | 적 300마리. 주석이 이미 *"a genuinely dynamic buffer waits until a scene needs more than this"*라고 트리거를 예고해 둠 |
| 인스턴싱 없음 — `DrawIndexedInstanced` 3곳 전부 instance count 1 | 동일 메시 수백 개 |
| 컬링 없음 — `BuildDrawQueues`가 모든 DrawItem을 매 프레임 전량 제출 | 아레나 밖 엔티티, 그림자 패스의 중복 제출 |
| `kSrvHeapCapacity = 64` (`Graphics/Renderer.h`) — ImGui·씬 SRV·모든 텍스처가 공유 | 적 타입과 프랩 텍스처 |
| `Transform`이 오일러각 (`Game/Components.h`) | 애니메이션 블렌딩. 주석이 이미 *"a quaternion becomes worth it once things start interpolating between orientations"*라고 조건을 명시 |
| `Vertex`에 본 인덱스·웨이트 없음 (`Graphics/Mesh.h`) | 스킨드 캐릭터 |
| 싱글 스레드 — 스레드 관련 심볼이 저장소에 0개 | 수천 엔티티 시뮬레이션 |

### 장르가 강제하는 것 두 가지

1. **VFX는 선택이 아니라 필수다.** 뱀서류의 정체성 대부분이 투사체·장판·피격
   피드백의 가독성에 있다. 지금 엔진에 파티클은 0개고, 이것 없이는 "돌아가지만
   죽은 게임"이 된다. M4에서 다룬다.

2. **PBR이 아니라 툰/NPR이다.** 가진 캐릭터 에셋은 램프 텍스처 + 아웃라인 전제로
   만들어진 것이라, 지금의 Blinn-Phong으로 그리면 "덜 예쁜" 게 아니라 **잘못 그린
   것**처럼 보인다. 렌더링의 목적지를 PBR이 아닌 툰 셰이딩 + 아웃라인 패스로 잡는다.
   PBR은 이 게임의 forcing function이 아니므로 별도 트리거를 기다린다.

### 알면서 받아들이는 약점

**적 다양성.** 가진 캐릭터는 전부 인간형이라 몬스터 웨이브로 쓰면 시각적 변별력이
떨어진다. 스케일·틴트·이동 속도 변형으로 완화하되 완전히 풀리지는 않는다. 웨이브
구성이 실제로 구분되지 않는다고 느껴지면 그때 몬스터 메시를 구한다.

### 도입할 외부 의존성

| 대상 | 선택 | 이유 |
|---|---|---|
| FBX 임포트 | **ufbx** | MIT, `.c`/`.h` 두 파일, 외부 의존성 0. 빌드 체계를 건드리지 않고 ImGui와 같은 방식으로 벤더링된다. Assimp는 CMake를 들여야 하고, Autodesk FBX SDK는 독점 EULA + 재배포 DLL이라 패키징 검증까지 손봐야 한다 |
| 물리 | **Jolt** | MIT, C++17, 외부 의존성 0 — `.cpp` 파일을 vcxproj에 넣으면 끝난다. PhysX 5도 BSD-3로 라이선스는 문제없지만 packman과 자체 생성 스크립트를 통과해야 해 단독 벤더링이 훨씬 무겁다. 기능 우열이 아니라 **순수 MSBuild 저장소에 덜 침습적인 쪽**으로 갈렸다. `JobSystemSingleThreaded`가 있어 스레드가 0개인 지금 상태로 붙였다가 M6에서 교체할 수 있는 것도 이 선택의 조건이다 |
| 셰이더 컴파일 | **DXC** | SM 6 없이는 bindless도 wave intrinsic도 없다. 선택지가 사실상 없음 |
| 이미지 코덱 | **stb_image 류** | WIC에 TGA 코덱이 없어 기존 트리거 대기 항목이 걸려 있다 |
| 오디오 | 미정 | M4에서 결정 |

---

## 마일스톤

| 마일스톤 | 주제 | 결과물 | 태그 |
|---|---|---|---|
| M1 | 몰려오는 적 | 절차 메시로 실제 플레이되는 최소 루프, 적 1000마리 | `v1.4` |
| M2 | 캐릭터 | 캐릭터가 애니메이션으로 뛰어다님 | `v1.5` |
| M3 | 월드와 물리 | 프랩이 놓인 아레나, 충돌과 점프 | `v1.6` |
| M4 | 게임의 얼굴 | 이펙트, 툰 셰이딩, HUD, 사운드 | `v1.7` |
| M5 | 에디터 | 에디터만으로 아레나와 웨이브 구성 | `v1.8` |
| M6 | 규모와 배포 | 멀티스레드, 배포 가능한 패키지 | `v2.0` |

---

## M1 — 몰려오는 적 (`v1.4`)

**목표**: 게임 루프를 절차 메시(캡슐·구)로 먼저 세운다. 캐릭터도 물리도 없이.
그리고 적 수를 올려서 상한들을 실제로 터뜨린 다음, 그것을 고치는 것이 이
마일스톤의 나머지다.

**순서가 중요하다** — 게임 루프를 먼저 세우는 이유는, 그래야 각 상한이 *어떤
수치에서, 어떤 증상으로* 터지는지 실측할 수 있기 때문이다. 상한부터 고치면 얼마나
필요한지 모르는 채로 크기를 정하게 된다.

### 게임 루프

- [ ] 탑다운 추적 카메라 — 기존 `CameraComponent`와 `ActiveCamera` 태그 재사용
- [ ] 플레이어 이동 (기존 `InputContext`), 캡슐 메시
- [ ] 적 스폰 — 화면 밖 링에서 시간 곡선에 따라
- [ ] 적 추적 이동, 접촉 데미지
- [ ] 자동 공격 — 사거리 내 최근접 적
- [ ] HP·사망·생존 타이머, XP 드롭과 획득

### 터지는 것들

- [ ] **Object CB 동적화** — 기존 트리거 대기 항목 발화. 10.2에서 32 → 256으로
      올려 시간을 벌었을 뿐이고, `UpdateObjectConstants`가 `kMaxObjects`를 넘기면
      예외를 던진다. GPU가 읽는 중인 버퍼의 재할당이라 RT 재생성과 같은 지연 처리가
      필요하다 — `RenderFrame` 상단의 기존 deferred resize 패턴을 재사용한다
- [ ] **인스턴싱** — 같은 메시 + 같은 머티리얼의 DrawItem을 하나의 draw call로.
      `PsoRole` 테이블에 인스턴스 variant를 추가한다 (지금 `[MSAA][role]` 2차원
      배열이라 확장 지점이 명확하다). 오브젝트별 root CBV + descriptor table 재바인딩이
      사라지는 것이 draw call 감소보다 큰 효과일 수 있으므로 둘을 나눠 측정한다
- [ ] **프러스텀 컬링** — `BuildDrawQueues`에서 기존 `Mesh::bounds`(AABB) 재사용.
      메인 패스와 그림자 패스는 절두체가 다르므로 각각 판정한다
- [ ] **SRV 힙 상한 제거** — `kSrvHeapCapacity = 64`를 ImGui·씬 컬러 SRV·그림자
      SRV·모든 텍스처가 나눠 쓰고 있어 텍스처 ~55장이면 에디터가 죽는다.
      `DescriptorAllocator`가 예외를 던지는 구조이므로 상한 제거 대상

### 선행 작업

- [ ] **파일 로그 채널** — Player는 실패하면 정보가 0이다(예외가 프로세스를 죽이고
      ImGui 패널이 없다). 수백 엔티티를 다루기 전에 필요하다. `Engine.lib`에 두고
      Editor/Player 양쪽이 쓴다

**검증**: 적 N = 100 / 500 / 1000 / 2000에서 프레임타임(median/p95/max)을 각 변경
전후로 측정한 표. 인스턴싱과 컬링은 따로 측정해 각각의 기여를 분리한다.
Debug Layer 0. 기존 회귀 테스트 전량 통과.

**완료 기준**: 적 1000마리가 몰려오는 아레나에서 60 Hz를 유지하며 실제로 5분
생존을 시도할 수 있다.

---

## M2 — 캐릭터 (`v1.5`)

**목표**: 캡슐이 캐릭터가 된다. FBX 임포트와 스켈레탈 애니메이션.

- [ ] `ThirdParty/ufbx` 벤더링, 버전 고정. `Engine.vcxproj`에 편입
- [ ] FBX 임포트 — 메시·스켈레톤·스킨 웨이트·머티리얼. 기존 OBJ 경로는 유지하고,
      `MeshData`/`SubmeshData` 계약에 맞춰 들어온다
- [ ] **`Vertex` 확장 — 본 인덱스·웨이트.** 결정해야 할 것: 절차 메시와 하나의
      vertex 계약을 공유할지, 스킨드 전용 스트림을 나눌지. 지금 `CreateMesh`와
      `GenerateTangents`는 *"네 필드가 모두 유효하다"*를 계약으로 삼고 있고 절차
      메시·파일 메시가 같은 경로를 지나는 것이 그 계약의 값어치다. 스킨 데이터를
      같은 구조체에 넣으면 절차 메시가 매 정점 16바이트를 낭비하고, 나누면 경로가
      둘이 된다 — 어느 쪽이든 그 대가를 명시하고 고른다
- [ ] **`Transform`을 쿼터니언으로.** 오일러각으로는 애니메이션 보간을 못 한다.
      주석이 이미 이 조건을 예고해 뒀다. Scene 포맷과 Inspector가 함께 바뀐다
- [ ] 애니메이션 클립 임포트·샘플링·포즈 계산
- [ ] 상태 전환과 블렌딩 — idle ↔ walk ↔ run ↔ attack. 크로스페이드
- [ ] **GPU 스키닝** (본 팔레트). 수백 스킨드 캐릭터가 목표이므로 CPU 스키닝은
      시작부터 제외한다. 팔레트를 CB로 둘지 structured buffer로 둘지는 캐릭터 수와
      본 수로 판단
- [ ] `SkinnedMeshRenderer` / `Animator` 컴포넌트, **Scene 포맷 v6**. 기존
      하위호환 규칙(`kSceneVersion`)을 그대로 따른다
- [ ] 이미지 코덱 도입 — 트리거 대기 항목 두 개(`map_Kd` 후보 확대, TGA 디코더)가
      함께 해소된다

**검증**: 캐릭터가 걷기·뛰기로 이동하고 에디터에서 클립을 전환할 수 있다.
스킨드 캐릭터 N = 50 / 200 / 500 프레임타임. v5 씬이 v6에서 그대로 열린다.

---

## M3 — 월드와 물리 (`v1.6`)

**목표**: 평면 위를 미끄러지던 것이 장애물이 있는 아레나가 된다.

- [ ] `ThirdParty/Jolt` 벤더링 + 수제 `Jolt.vcxproj`. **`JobSystemSingleThreaded`로
      시작** — M6에서 `JobSystemThreadPool`로 교체한다
- [ ] Transform ↔ Body 동기화 계층, 고정 타임스텝. 렌더 프레임과 물리 스텝이
      다른 주기로 도는 순간 보간이 필요해진다
- [ ] **적 군중은 Jolt 바디로 만들지 않는다.** 1000개 강체를 솔버에 넣는 것은 잘못된
      도구다 — 적끼리는 겹치지만 않으면 되고 스택도 마찰도 필요 없다. Jolt는
      플레이어 `CharacterVirtual`과 정적 월드·프랩에만 쓰고, 적 분리는 공간 해시
      기반 자체 패스로 처리한다. 이 분리가 M6의 병렬화 대상이기도 하다
- [ ] 프랩 배치, 지형·장애물 충돌, 점프
- [ ] **Transform 부모-자식 계층** — 기존 트리거 대기 항목 발화. 프랩 조립과 본
      소켓(무기·이펙트 부착)이 둘 다 이것을 요구한다. 월드 행렬 계산 순서와
      Scene 포맷이 함께 바뀐다

**검증**: 아레나에서 장애물을 피하고 넘어다니며 5분 생존. 물리 스텝 비용을 적
수에 대해 측정. 계층 구조가 있는 씬의 저장/복원 왕복.

---

## M4 — 게임의 얼굴 (`v1.7`)

**목표**: 돌아가는 것에서 게임처럼 보이고 들리는 것으로. 이 마일스톤에서
셰이더 수가 3개에서 10개 이상으로 늘고 패스가 4개에서 8개 수준이 된다.

- [ ] **파티클 / 빌보드 시스템** — M1의 인스턴싱 인프라 재사용. 가산 블렌딩 PSO
- [ ] 투사체·장판·피격 피드백, 데미지 숫자
- [ ] **툰 셰이딩 + 아웃라인 패스** — 램프 텍스처, 셰이딩 단계화. 지금의 Blinn-Phong
      경로는 절차 메시·프랩용으로 남긴다
- [ ] **DXC / SM 6 전환** — 셰이더 수와 permutation이 여기서 늘어난다. 지금은
      `BuildSettings.props`에 셰이더 목록이 MSBuild 항목으로 손으로 유지되고 있어
      셰이더 추가가 XML 편집이다. `CompileRuntimeShader` 타깃과
      `PlayerPackage.contents`를 함께 고친다
- [ ] HUD, 레벨업 선택 UI — Player에는 ImGui가 없으므로 자체 UI 경로가 필요하다.
      이것이 이 마일스톤에서 가장 과소평가되기 쉬운 항목이다
- [ ] 오디오 — 라이브러리 선정 포함

**여기서 발화할 수 있는 것**: 리소스 상태 추적기. 지금 배리어는 패스마다 손으로
쓰고 상태는 `m_shadowMapIsShaderResource` 같은 ad-hoc bool로 추적한다. 4개
패스에서는 옳지만 8개에서는 무너진다. *트리거: 새 패스를 추가하다 배리어를 틀리는
순간.* 렌더 그래프 전체가 아니라 상태 추적기만으로 충분할 가능성이 높으므로,
발화하면 그 범위부터 검토한다.

**검증**: 이펙트 있는/없는 빌드 프레임타임 대조. 파티클 수에 대한 스케일 측정.
DXC 전환 후 기존 렌더링 결과가 픽셀 단위로 동일한지 확인 (셰이더 컴파일러 교체는
조용히 결과를 바꿀 수 있다).

---

## M5 — 에디터 (`v1.8`)

**목표**: 콘텐츠를 만들 도구. 여기까지 오면 만들 것이 생겨서 도구가 값어치를 갖는다
— 반대 순서였다면 과잉 투자였다.

- [ ] 트랜스폼 기즈모 (이동·회전·크기)
- [ ] 언두 / 리두 — 기존 `EditorCommand` 지연 적용 구조가 자연스러운 접점이다
- [ ] 다중 선택
- [ ] **프리팹** — 적 타입과 프랩의 재사용. Scene 포맷 확장. 웨이브 정의가 여기에 얹힌다
- [ ] 메시 3D 썸네일 — 기존 트리거 대기 항목
- [ ] 비동기 에셋 로드 — 기존 트리거 대기 항목. 10.3에서 23 MB OBJ에 **5043 ms**
      측정, UI 스레드가 통째로 멈춘다. FBX 캐릭터가 들어오면 더 나빠진다

**검증**: 에디터만으로 아레나 하나와 적 웨이브를 구성한 뒤 Player로 플레이.
저장 → 종료 → 재시작 → 로드 왕복이 동일하게 복원된다.

---

## M6 — 규모와 배포 (`v2.0`)

**목표**: 스레드를 도입하고, 실제로 배포 가능한 패키지로 만든다.

- [ ] **잡 시스템** — 엔진 전체가 지금 싱글 스레드다. Jolt를 `JobSystemThreadPool`로
      전환하는 것과 자체 잡 시스템 중 무엇이 먼저인지 여기서 판단한다
- [ ] 엔티티 시뮬레이션 병렬화 — M3에서 분리해 둔 적 군중 패스가 첫 대상
- [ ] 멀티스레드 커맨드 기록 — 지금 `Renderer`는 커맨드 리스트 하나,
      `FrameResource`는 할당자 하나다. 둘 다 바뀐다
- [ ] Bindless (descriptor indexing) — M4의 DXC 전환이 선행 조건
- [ ] **플레이스홀더 캐릭터** — 절차 메시 + 자작 클립. 라이선스상 배포할 수 없는
      에셋 없이도 `VerifyPlayerPackage.ps1`이 통과해야 한다. 이것이 없으면 독립
      배포 검증이 개발 머신 전용 테스트가 된다
- [ ] 밸런스와 콘텐츠, 최종 패키지

**검증**: 저장소·솔루션·소스가 없는 빈 폴더에서 패키지 실행. 가능하면 개발 도구가
없는 다른 PC에서도. 스레드 수에 대한 스케일 측정.

---

## 트리거 대기 항목

미리 만들지 않고, 조건이 실제로 발생하는 순간 착수한다. 측정값은 트리거 판단의
근거이므로 그대로 보존한다.

**발화 예정** — 해당 마일스톤에서 조건이 발생할 것으로 예상. 트리거는 예상일 뿐
계약이 아니므로, 그 마일스톤에서 실제로 발생하지 않으면 대기로 되돌린다.

| 항목 | 트리거 | 발화 지점 |
|---|---|---|
| Object CB 동적화 | 한 씬이 `kMaxObjects`(256)를 넘기는 순간. 10.2에서 32 → 256으로 올려 시간을 벌었을 뿐 | M1 |
| 씬 계층 구조 (Transform 부모-자식) | 여러 엔티티를 한 덩어리로 움직이고 싶어지는 순간 | M3 |
| 비동기 에셋 로드 | 큰 모델을 고를 때의 정지가 실제로 작업을 방해하는 순간. 10.3에서 23 MB OBJ에 **5043 ms** 측정 — UI 스레드가 통째로 멈춘다 | M5 |
| 메시 3D 썸네일 | 파일 이름만으로 에셋을 구분하기 어려워지는 순간. 에셋마다 렌더 타겟이 필요하다 | M5 |
| `map_Kd` 경로 복구 후보 확대 | 확장자를 생략한 비-PNG 텍스처를 쓰는 모델이 들어오는 순간. 11.2.5의 `ResolveMtlTexture`는 후보가 `이름 그대로` / `+ ".png"` 둘뿐이다 — **후보 목록을 늘리는 것으로 끝나는 문제** | M2 |
| TGA 디코더 | TGA 텍스처를 쓰는 모델이 들어오는 순간. **위와 다른 문제다** — 경로를 찾아도 Windows 기본 WIC에 TGA 코덱이 없어 `CreateDecoderFromFilename`이 실패한다. 후보 목록이 아니라 디코더를 붙여야 한다 | M2 |

**대기 유지**:

- [ ] **렌더 타겟 리사이즈의 프레임별 폐기 큐** — *트리거: 뷰포트 스플리터를 끄는
      동안의 전체 플러시가 거슬리는 순간.* 11.7 연속 측정에서 안정 시 약 6.05 ms,
      resize 시 4x median/p95/max가 13.95 / 14.59 / 15.64 ms, 1x가
      13.83 / 14.69 / 15.46 ms였다. 두 모드가 같으므로 원인은 MSAA가 아니라 기존
      `WaitForGpu()` full flush다. 최대값이 60 Hz 예산 안이고 입력 손실도 없어
      대기하되, 165 Hz 편집에서 약 70 fps로 떨어지는 것이 거슬리면 발화

- [ ] **삼각형 단위 픽킹 (또는 겹친 후보 순환 선택)** — *트리거: 큰 메시의 AABB가
      그 안의 오브젝트를 가려 클릭으로 못 고르는 상황이 실제로 작업을 방해할 때.*
      11.0에서 확인: 약 21 단위 상자가 그 안의 오브젝트를 통째로 삼켰다. 최근접 규칙
      자체는 정확하고, 뾰족한 모델에 상자를 씌운 결과다. 순환 선택이 삼각형 교차보다
      훨씬 싸다. M5의 기즈모가 들어오면 증상이 완화될 수 있어 그 뒤에 재평가한다

- [ ] **OBJ 업 축(up-axis) 보정** — *트리거: Z-up 좌표 그대로 들어온 모델을 실제로
      불러와 누워버리는 순간.* OBJ에는 신뢰할 수 있는 up-axis 메타데이터가 없고
      AABB로도 추론할 수 없어, 자동 판정이 아니라 **에셋별 import 설정**이 필요하다.
      **순서가 핵심** — 배치 후에 회전시키면 `-min.y` 안착 계산이 무효가 되므로
      변환이 bounds 계산보다 먼저 와야 한다:
      `import-axis 변환 → 변환된 bounds → size fit → center/ground 배치`.
      **우선순위 하락** — M2 이후 FBX가 주 경로가 되고 ufbx는 축 변환을 자체
      지원한다. OBJ에만 남는 문제가 된다

- [ ] **PBR** — *트리거: 툰 셰이딩이 맞지 않는 콘텐츠가 실제로 필요해지는 순간.*
      이 게임의 forcing function이 아니다

- [ ] **리소스 상태 추적기** — *트리거: 새 패스를 추가하다 배리어를 틀리는 순간.*
      M4에서 발화 가능성이 높다

**폐기**:

- ~~Assimp 도입~~ — ufbx로 대체. Assimp는 CMake를 빌드 체계에 들여야 하는데,
  필요한 것은 FBX 하나이고 ufbx가 빌드 체계를 건드리지 않고 그것을 준다

---

## 커밋 / 브랜치 전략

- **main 브랜치 단일 운용.** 실험적 리팩토링만 브랜치를 판다
- 커밋 메시지: 관례적 접두어(`feat:` / `fix:` / `refactor:`) + 증상 → 원인 → 수정 →
  **실측 검증**을 본문에
- 회귀 푸터 유지: `회귀: 테스트 N/N, 독립 배포 검증 통과, 경고 0, 오류 0.`
- 마일스톤 완료 시 태그 (`v1.4` ~ `v2.0`)

## 참고 자료

- [DirectX 12 공식 문서](https://learn.microsoft.com/en-us/windows/win32/direct3d12/direct3d-12-graphics)
- [Microsoft DirectX-Graphics-Samples](https://github.com/microsoft/DirectX-Graphics-Samples) — 고급 샘플(D3D12MeshShaders, DynamicIndexing 등)
- [DirectXShaderCompiler](https://github.com/microsoft/DirectXShaderCompiler) — SM 6 전환 (M4)
- [Jolt Physics](https://github.com/jrouwe/JoltPhysics) — 물리 (M3). `Docs/Architecture.md`와 `Samples/`가 통합 시 주 참고자료
- [ufbx](https://github.com/ufbx/ufbx) — FBX 임포트 (M2)
- [WIKI.md](WIKI.md) — 에디터 조작, Scene·에셋, 배포, 문제 해결
- [Engine/Docs/](Engine/Docs/) — 실행·프로젝트·패키지 경계 계약
